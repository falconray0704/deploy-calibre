# deploy-calibre
Deploy Calibre and Calibre-web by docker compose.

```bash
./run.sh 
Usage:
./run.sh -c <cmd> -l "<item list>"
eg:
./run.sh -c deploy -l "calibre"
Supported cmd:
deploy
Supported items:
calibre
```

